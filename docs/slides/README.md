# สื่อนำเสนอ (สำหรับ Canva)

สามชุด ใช้ธีมเดียวกันจาก `slide_theme.py` แปะต่อกันในเด็คเดียวได้

| ชุด | โฟลเดอร์ | สร้างด้วย |
|---|---|---|
| **รายงานความก้าวหน้า ครั้งที่ 1 · 9 สไลด์** | `progress-slides/` | `gen_progress_slides.py` |
| การออกแบบฐานข้อมูล · 11 ภาพ | `schema-cards/` | `gen_schema_cards.py` |
| การออกแบบส่วนติดต่อผู้ใช้ · 12 สไลด์ | `ui-slides/` | `extract_screens.py` แล้ว `gen_flow_slides.py` |

---

# ชุดที่ 3 — สไลด์รายงานความก้าวหน้า (`progress-slides/`)

9 สไลด์ 16:9 (1600×900) ย่อจาก `docs/progress-report-01.tex` **เฉพาะหัวข้อ 2, 4, 5, 7**

```bash
cd ~/Projects/SoftwareEn/docs/slides
python3 gen_progress_slides.py          # ได้ .svg อย่างเดียว
python3 gen_progress_slides.py --png    # ได้ .png ด้วย (ไว้ตรวจงานด้วยตา)
```

| # | สไลด์ | หัวข้อในรายงาน |
|---|---|---|
| 00 | หน้าปก — ULMs · 43% เทียบแผน 42% | — |
| 01 | ภาพรวม 6 Sprint เป็นไทม์ไลน์ พร้อมหมุด "อยู่ตรงนี้" | §2 |
| 02 | กราฟแท่งรายมิติ แผน vs ผลจริง | §4 |
| 03 | ตารางรายมิติ + ข้อเท็จจริงประกอบ + ข้อสังเกต | §4 |
| 04 | กราฟเทียบแผน (S-curve) + วิธีอ่าน 3 ข้อ | §5 |
| 05 | ภาพรวมผลงานที่ส่งมอบ 4 ก้อน | §7 |
| 06 | ฝั่ง Back-End | §7 |
| 07 | ฝั่ง Front-End | §7 |
| 08 | เครื่องมือ กระบวนการ และเอกสาร | §7 |

## สองข้อตกลงของเด็คนี้ — อย่าเผลอใส่กลับ

**ไม่มีหัวข้อ "ข้อจำกัดที่เหลือ"** เด็คนี้เล่าเฉพาะผลงานที่ส่งมอบแล้ว ข้อจำกัดยังอยู่ครบใน
รายงาน `progress-report-01.tex` ใต้ผลงานแต่ละฝั่ง — ถ้าอาจารย์ถามระหว่างนำเสนอ
ให้เปิดรายงานตอบ ไม่ต้องเติมกลับเข้าสไลด์

**ไม่ใช้จุดคั่น `·` ในข้อความที่ขึ้นสไลด์** คั่นวลีด้วยการเว้นวรรค หรือใช้คำเชื่อม
("และ" / "พร้อม") แทน ถ้าเว้นวรรคแล้วอ่านกำกวมให้เขียนคำเชื่อมลงไป อย่ากลับไปใช้จุด
(จุดที่ยังเหลือใน `.py` เป็นคอมเมนต์กับเส้นคั่นหัวข้อในโค้ด ไม่ได้ขึ้นสไลด์)

**หน้าปกตัดทิ้งได้** ถ้าเอาไปแปะต่อในเด็คที่มีปกอยู่แล้ว — ลบ `00-cover.svg` ทิ้งอย่างเดียวพอ
ไฟล์อื่นไม่ได้อ้างถึงกัน

## แก้เนื้อหา

ถ้อยคำและตัวเลขทั้งหมดอยู่ใน **`progress_data.py`** ไฟล์เดียว `gen_progress_slides.py`
มีแต่โค้ดวาด — แก้ข้อความแล้วรันใหม่ ไม่ต้องแตะโค้ด SVG

ตัวเลขอ้างสถานะ ณ commit `50864c3` (13 ส.ค. 2569) ตรงกับรายงาน **ถ้ารายงานเปลี่ยน
ต้องมาแก้ที่นี่ด้วย** สไลด์ไม่ได้อ่านค่าจาก `.tex` โดยตรง

## การตัดบรรทัดภาษาไทย

ภาษาไทยไม่มีช่องว่างระหว่างคำ **สไลด์ตัดบรรทัดได้เฉพาะตรงที่เว้นวรรคไว้เองใน
`progress_data.py`** เวลาเขียนคำบรรยายจึงต้องเว้นวรรคตามวลี ไม่ใช่ตามความสวยของ
บรรทัดในไฟล์ ถ้าลืมเว้น ข้อความจะถูกตัดกลางคำเป็นทางสุดท้าย (ดู `_split_token`)

ความกว้างวัดจาก advance width จริงของฟอนต์ผ่าน `text_metrics.py` ไม่ใช่เดาจากจำนวน
ตัวอักษรแบบ `thai_w()` ใน `slide_theme.py` — **ต้องมี Pillow ที่มี Raqm** ไม่งั้นสระบนล่าง
กับวรรณยุกต์จะถูกนับความกว้างเกินจริง แล้วบรรทัดจะสั้นกว่าที่ควร ตรวจด้วย

```bash
python3 -c "from PIL import features; print(features.check('raqm'))"
```

## PNG บนเครื่องนี้

`rsvg-convert` (แพ็กเกจ `librsvg2-bin`) **ไม่ได้ติดตั้ง** ชุดนี้จึงใช้ `svg_to_png.py`
ซึ่งเรียก librsvg ผ่าน GObject Introspection แทน ตัวเรนเดอร์เป็นตัวเดียวกันจึงได้ผลเหมือนกัน
ส่วน `slide_theme.rasterize()` ที่อีกสองชุดใช้ยังต้องการไบนารีอยู่ (ไม่ได้แก้ ของเดิมยังทำงานได้ถ้าลงเพิ่ม)

## ฟอนต์ตอนอัปเข้า Canva

SVG อ้างฟอนต์ **Laksaman** กับ **DejaVu Sans Mono** ถ้า Canva ไม่มี จะถูกแทนด้วย
ฟอนต์อื่นแล้วระยะห่างจะเพี้ยน (ตำแหน่งทุกอย่างคำนวณจากความกว้างของฟอนต์จริง)
**ถ้าไม่ตั้งใจจะแก้ข้อความต่อใน Canva ให้อัป `.png` แทน จะตรงตามที่ออกแบบไว้เสมอ**

---

# ชุดที่ 2 — สไลด์ UI/UX (`ui-slides/`)

12 สไลด์จาก `resource/ULMS_FlowBoard_standalone.html` (18 หน้าจอ · เส้นเชื่อม 23 เส้น)
ไล่ตามเส้นทางการยืมจริง ไม่ได้ไล่ตามบทบาท

| # | สไลด์ | ชนิด | หน้าจอ |
|---|---|---|---|
| 01 | ผังหน้าจอทั้งระบบ | grid | ทั้ง 18 จอ |
| 02 | เข้าสู่ระบบ และบทบาทผู้ใช้ | zoom | `login` |
| 03 | ค้นหาและเลือกครุภัณฑ์ | flow | `home → catalog → item` |
| 04 | **สร้างคำขอยืม** | zoom | `request` |
| 05 | การอนุมัติคำขอ | flow | `request → approve → queue` |
| 06 | รับของและคืนของ | flow | `myreq → pickup → return` |
| 07 | **ตรวจสภาพและการหักเครดิต** | zoom | `inspect` |
| 08 | แดชบอร์ดเจ้าหน้าที่ | zoom | `dash` |
| 09 | คลังครุภัณฑ์และการลงทะเบียน | flow | `stock → additem` |
| 10 | เครดิตและประวัติผู้ยืม | flow | `profile · borrower` |
| 11 | ตั้งค่าระบบ | zoom | `admin` |
| 12 | ระบบออกแบบร่วม | zoom | `kit` |

**สองสไลด์ที่ควรใช้เวลามากที่สุดคือ 04 กับ 07** — จอ `request` รวมกติกาการแบ่งระดับ
T0–T3 ไว้ครบในหน้าจอเดียว และจอ `inspect` แสดงสูตรหักเครดิต
(น้ำหนักอุปกรณ์ × ระดับความเสียหาย → คะแนนที่หัก → อายุบทลงโทษ) ให้เห็นกับตา
ทั้งคู่ตรงกับ §5.4 และ §5.6 ของรายงาน

### สามชนิดสไลด์ และเหตุผล

ภาพหน้าจอทุกจอกว้าง 1440 px ถ้าย่อลงเต็มสไลด์ ตัวหนังสือจะเหลือราว 9–11 px
บนโปรเจกเตอร์คืออ่านไม่ออก แต่ละชนิดจึงแก้ปัญหานี้คนละแบบ

- **grid** — thumbnail ทั้ง 18 จอ ไม่ได้ให้อ่าน ให้เห็นขนาดของระบบและการแบ่งบทบาท
- **flow** — 2–3 จอ + ลูกศร + ป้ายที่ดึงมาจาก `EDGES` ในบอร์ดเอง ให้เห็นลำดับ
- **zoom** — จอเดียว **ครอปแถบเมนูซ้าย 240 px ทิ้ง เหลือเนื้อหา 1200 px แล้วแสดงที่ 1:1**
  ตัวหนังสือจึงคมเท่าต้นฉบับ ที่เหลือเป็นคอลัมน์คำอธิบายพร้อมหมุดเลขบนภาพ
  หมุดวางในที่ว่างแล้วลากเส้นไปยังจุดที่ชี้ ไม่วางทับของที่กำลังอธิบาย

### สีของภาพหน้าจอ

mockup ใช้ **เขียว KU** ซึ่งคนละสีกับ teal `#16726D` ของเด็ค — ตั้งใจปล่อยไว้ตามนั้น
เพราะภาพคือ UI จริงที่ทีมทำ กรอบ หัวสไลด์ ลูกศร และหมุด ที่เราวาดเองต่างหากที่ใช้
สีของเด็ค

### ความถูกต้องของคำบรรยาย

คำบรรยายเขียนจากสิ่งที่ปรากฏในภาพจริง **ยกเว้นสี่จอ (`home` `item` `myreq` `queue`)
ที่ยังไม่ได้เปิดตรวจทีละจอ** — คำบรรยายของสี่จอนี้เขียนจากป้ายเส้นเชื่อมในบอร์ดเอง
จึงพูดถึงแค่บทบาทของจอในเส้นทาง ไม่ได้อ้างรายละเอียดในหน้าจอ ถ้าจะใช้จริงควรเปิด
ดูแล้วเติมให้เจาะจงขึ้น (แก้ที่ `CAPTIONS` ใน `flow_data.py`)

### ขนาดไฟล์

`ui-slides/` ~18 MB และ `screens/` ~1.9 MB เพราะฝังภาพหน้าจอไว้ในทุกไฟล์
ถ้าจะ commit ควรตัดสินใจก่อนว่าจะเอา PNG เข้า repo ด้วยหรือไม่ (ดู PROGRESS.md
หัวข้อขนาดของ `docs/`)

---

# ชุดที่ 1 — สื่อนำเสนอการออกแบบฐานข้อมูล

การ์ด schema ของ ULMs Database Design **v4** (33 ตาราง) แยกเป็น **9 กลุ่มตามฟีเจอร์**
กลุ่มละ 1 ไฟล์ ขนาดพอดีสำหรับแปะเป็นภาพเดียวต่อสไลด์

## ไฟล์ที่ได้ — `schema-cards/`

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `*.png` | ภาพความละเอียด 2.5× (กว้าง 2700 px) พื้นหลังโปร่ง มุมโค้ง — **ลากวางใน Canva ได้เลย** |
| `*.svg` | ไฟล์เวกเตอร์ตัวเดียวกัน สำหรับ Canva Pro (Upload → SVG) เผื่ออยากแก้สี/ฟอนต์ต่อ |

> การ์ดใช้ฟอนต์ **DejaVu Sans Mono** (ชื่อฟิลด์) กับ **Laksaman** (ข้อความไทย) ถ้า Canva
> ไม่มีฟอนต์เหล่านี้ ตัว SVG จะถูกแทนด้วยฟอนต์อื่นแล้วระยะห่างจะเพี้ยน — **ถ้าไม่ตั้งใจจะ
> แก้ต่อ ให้ใช้ `.png` จะตรงตามที่ออกแบบไว้เสมอ**
| `*.md` | ตารางฟิลด์แบบข้อความ สำหรับก๊อปไปทำ speaker note หรือพิมพ์ลงตารางของ Canva เอง |

| # | กลุ่ม | ตาราง |
|---|---|---|
| 00-map | *แผนที่ความสัมพันธ์ทั้งฐานข้อมูล* | 33 ตาราง + FK ทั้ง 58 เส้น เอาแค่ชื่อตาราง |
| 00-overview | *สารบัญ 9 กลุ่ม* | รายชื่อตารางแบบข้อความ ไม่มีเส้น |
| 01 | บัญชีผู้ใช้และสิทธิ์ | AccountInfo · RoleInfo · Authority · AuthorityRole |
| 02 | หน่วยงานผู้ให้ยืม | FacultyInfo · ClubInfo · ManagementGroup · GroupType |
| 03 | ทรัพยากร (อุปกรณ์+ห้อง) | ItemInfo · ItemIndiv · ResourceStatus · ResourceInfo · RoomInfo |
| 04 | กฎการยืมและเครดิต | BorrowRule · BorrowConstraints · CreditTier · Eligibility |
| 05 | การจองและการอนุมัติ | Reservations · ApproveStatus |
| 06 | การยืม–คืนและต่อเวลา | UsageLog · ExtensionRequest |
| 07 | การตรวจสภาพและรูปภาพ | Inspection · ConditionLog · Image · ImageSubmissionType · ConditionType |
| 08 | บทลงโทษและการอุทธรณ์ | PenaltyRule · PenaltyInfo · AppealInfo · PenaltyType |
| 09 | งานสนับสนุน | RepairLog · Notification · NotificationType |

การจัดกลุ่มนี้ **ไม่ตรงกับหัวข้อในเอกสารต้นทาง** โดยตั้งใจ — เอกสารต้นทางแบ่งตาม
โครงสร้าง (เช่นรวม `Eligibility` ไว้กับกลุ่มบทลงโทษ) แต่สไลด์ต้องเล่าเป็นเรื่อง จึงแบ่ง
ตามฟีเจอร์ที่ตารางรับผิดชอบแทน ส่วนพจนานุกรมข้อมูลในรายงาน (§5.13) ยังคงใช้การแบ่ง
ตามเอกสารต้นทางเหมือนเดิม

## แผนที่รวม (`00-map`)

สไลด์เดียวเห็นทั้งฐานข้อมูล ใช้เปิดหัวข้อก่อนเจาะเข้าสไลด์ 01–09 — กล่องหนึ่งกล่องคือ
หนึ่งกลุ่มพร้อมรายชื่อตารางในกลุ่ม เส้นคือความสัมพันธ์ **ระดับกลุ่ม**

**ทำไมถึงเป็นระดับกลุ่ม ไม่ใช่ระดับตาราง** — รอบแรกวาด FK รายตารางครบทั้ง 58 เส้น
แล้วอ่านไม่ออก เส้นพันกันจนตามไม่ทัน พอยุบคู่ที่อยู่กลุ่มเดียวกันทิ้ง (22 เส้น เพราะ
สไลด์ย่อยของกลุ่มนั้นวาดให้แล้ว) เหลือ **22 เส้นระหว่างกลุ่ม** จาก FK 36 เส้น
ความหนาของเส้นและตัวเลขในวงกลมบอกว่าคู่นั้นเชื่อมกันด้วย FK กี่เส้น

กล่องวางบนตาราง **4 คอลัมน์ × 3 แถว เรียงตามเลขกลุ่ม 01→09** ซึ่งเป็นลำดับเดียวกับ
สไลด์ที่จะพูดต่อ พอเทียบผังที่เป็นไปได้แล้วผังนี้ให้เส้นรวมสั้นกว่าผัง 2×5 ที่ล็อกตาม
lifecycle ราว 20% ด้วย (ผังที่เครื่องจัดเองอิสระสั้นกว่านี้อีก แต่สลับลำดับกลุ่มจนเล่าไม่ได้)
ตำแหน่งอยู่ที่ `slot` ใน `schema_data.py` ส่วน `map_order` คุมลำดับชื่อตารางในกล่อง

## พาเลตต์

สีทั้งหมดดึงมาจากเด็คนำเสนอ `resource/ULMs_Software_Project_Presentation_V.pdf`
(teal `#16726D` เป็นสีหลัก · ส้ม `#FD914D` เป็นสีเน้น · พื้นอ่อน `#F2F7F7`) แต่ละกลุ่ม
ได้เฉดของตัวเองในตระกูล teal–เขียว ยกเว้นกลุ่ม **08 บทลงโทษและการอุทธรณ์** ที่ใช้ส้ม
เพื่อให้อ่านออกว่าเป็นเรื่องบทลงโทษตั้งแต่แรกเห็น และกลุ่ม **09 งานสนับสนุน** ที่ใช้ slate

ทุกสีถูกทำให้เข้มพอที่ตัวอักษรสีขาวบนหัวการ์ดจะได้ contrast **≥ 4.5:1** ตาม WCAG AA
(ส้มของเด็คคือ `#FD914D` ซึ่งอ่อนเกินไป จึงเข้มลงเป็น `#AD4E0C`) — สำคัญเพราะสไลด์
ต้องอ่านออกบนโปรเจกเตอร์ที่คอนทราสต์ต่ำกว่าจอคอม ถ้าจะเปลี่ยนสีเองในอนาคต ให้เช็ค
ค่านี้ก่อนทุกครั้ง

## วิธีอ่านการ์ด

- ป้าย **PK** สีทอง = Primary Key · ป้าย **FK** ขอบสีเน้น = Foreign Key
- **เส้นโค้ง** = FK ที่ชี้ไปตารางอื่น *ในกลุ่มเดียวกัน* (จุดออกที่ฟิลด์ → หัวลูกศรที่ตารางปลายทาง)
- **`→ ชื่อตาราง`** สีเทาหลังชื่อฟิลด์ = FK ที่ชี้ออกไป *นอกกลุ่ม* วาดเส้นในสไลด์นี้ไม่ได้

## แหล่งข้อมูล

- โครงสร้างตารางทั้งหมดมาจาก `resource/DatabaseDesign4.pdf` (เอกสารเป็นภาพล้วน
  `pdftotext` อ่านไม่ได้) ซึ่งถูกถอดความไว้แล้วใน `sections/05_scope.tex` §5.13
- ปลายทางของ FK ที่เอกสารต้นทางเขียนแค่ว่า "FK" โดยไม่ระบุตาราง (เช่น
  `RepairLog.ConditionBeforeRepair`, `UsageLog.CheckoutCondition`) ตรวจสอบกับ
  `backend-preview/backend/prisma/schema.prisma` ซึ่งเป็น v4 ที่ migrate จริงแล้ว
  ทั้งหมดชี้ไป `ConditionLog`

## วิธีสร้างใหม่

```bash
cd ~/Projects/SoftwareEn/docs/slides
python3 gen_schema_cards.py
```

แก้ฟิลด์/คำอธิบาย/การจัดกลุ่มได้ที่ `schema_data.py` แล้วรันสคริปต์ใหม่ ทุกไฟล์จะถูก
สร้างทับทั้งชุด

**ต้องมี `rsvg-convert` ก่อน** (`sudo apt install librsvg2-bin`) — ตัวแปลง SVG→PNG
ตัวอื่นบนเครื่องนี้ใช้ไม่ได้: Inkscape ที่ติดตั้งไว้เป็น snap และพังจาก glibc ไม่ตรงกัน,
gdk-pixbuf ของ Ubuntu 26.04 โหลด SVG ผ่านโพรเซสแยกที่ล้มเมื่อไม่มี session bus, ส่วน
ตัวเรนเดอร์ในตัวของ ImageMagick วาดเส้นแบบ stroke-only ไม่ได้ (เส้น FK กลายเป็นก้อนดำ)
ถ้ามีไบนารีอยู่ที่อื่นให้ชี้ด้วย `RSVG_CONVERT=/path/to/rsvg-convert python3 gen_schema_cards.py`
