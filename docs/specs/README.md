# `specs/` — SRS และ SDS ฉบับ Word และ PDF

โฟลเดอร์นี้สร้างเอกสารสองฉบับที่ **อาจารย์เปิดใน MS Word ได้จริง** พร้อม PDF
ที่แปลงมาจากไฟล์ `.docx` ตัวเดียวกัน (ไม่ใช่คนละต้นฉบับ)

> เอกสารอื่นทั้งหมดของโครงการเขียนด้วย LaTeX ที่ `docs/` — ดู `docs/INDEX.md`
> ที่นี่ใช้ Word เพราะโจทย์คือ "ต้องแก้ต่อในเวิร์ดได้"

## วิธีสร้าง

```bash
cd docs/specs
uv run --with python-docx --python 3.12 python gen_srs_docx.py
uv run --with python-docx --python 3.12 python gen_sds_docx.py
```

ผลลัพธ์ออกที่ `docs/build/` ทั้งสี่ไฟล์

| ไฟล์ | คืออะไร |
|---|---|
| `build/ULMs-SRS-v1.1.docx` / `.pdf` | ข้อกำหนดความต้องการ 24 หน้า (ฉบับแก้ไข 1.1) |
| `build/ULMs-SDS-v1.3.docx` / `.pdf` | ข้อกำหนดการออกแบบ 50 หน้า (ฉบับก่อนหน้าอยู่ที่ `report/`) |

## โครงเอกสารมาจากสไลด์ของวิชา ห้ามเปลี่ยนเอง

- **SDS** → `resource/Week 09 - System Design Specification 2569-08-27 R12.pdf` สไลด์ 29–33
  กำหนดหัวข้อ 1–11 ไว้ตายตัว (General Product Description → Back-out Procedure)
  `sds_data.py` เรียงตามนั้นทั้งหมด **เพิ่มหัวข้อย่อยได้ แต่อย่าสลับหรือตัดหัวข้อหลัก**
- **SRS** → `resource/Week 05 - Requirements Gathering 2569-07-23 R10.pdf` กำหนดกรอบ IEEE 830-1998
  `srs_data.py` เรียงตามนั้นแล้ว **หัวข้อ 1.5, 2.1.1-2.1.8, 2.6, 3.3, 3.5, 3.6 เขียนเพิ่มใหม่**
  เพราะฉบับของทีมไม่มี ส่วนความต้องการทุกข้อยังเป็นของทีมคำต่อคำ

## ไฟล์ในโฟลเดอร์นี้

| ไฟล์ | หน้าที่ |
|---|---|
| `srs_data.py` | **เนื้อหา SRS** — แก้ข้อความที่นี่ที่เดียว |
| `sds_data.py` | **เนื้อหา SDS** — แก้ข้อความที่นี่ที่เดียว |
| `doc_builder.py` | รูปแบบทั้งหมด (ฟอนต์ ตาราง สารบัญ หัว-ท้ายกระดาษ แปลง PDF) |
| `gen_srs_docx.py` / `gen_sds_docx.py` | ตัวสั่งงาน อย่างละ 4 บรรทัด |

**อย่าแก้ `.docx` ใน `build/` โดยตรง** — รันใหม่ทีเดียวแล้วหาย
ถ้าจะแก้ถ้อยคำให้แก้ที่ `*_data.py` แล้วสร้างใหม่

> **SDS ฉบับ 1.2 (17 ก.ย.) สร้างจาก generator ล้วน** — การแก้ด้วยมือของฉบับ 1.0
> (3 ก.ย.) ถูกย้ายเข้า `sds_data.py` เป็นกติกาการเขียนแล้ว: ไม่ใช้ `·` คั่นรายการ
> ไม่มีหน้าสรุปผู้ตรวจ ไม่มีหัวข้อนิยาม เอกสารอ้างอิง และภาคผนวกสอบทาน
> ตัดประโยคอธิบายเหตุผลออก เส้นทางหลายขั้นเขียนขั้นละบรรทัด (`"\n"` ในช่องตาราง)
> ย่อหน้าเยื้องบรรทัดแรกด้วย `PARAGRAPH_INDENT_CM` แทนการกด tab
> ฉบับ 1.0 ที่ส่งไปแล้วเก็บไว้ที่ `report/ULMs-SDS-v1.0.*` และ `build/ULMs-SDS-v1.0.*` ไม่ถูกทับ
> เพราะ `OUTPUT_STEM` เปลี่ยนเป็น `ULMs-SDS-v1.2`
>
## กฎการตัดหน้า อยู่ใน generator แล้ว

ฉบับ 1.0 และ 1.1 เคยจัดหน้าด้วยมือใน LibreOffice ตอนนี้ `doc_builder.py` ทำให้เอง 3 ข้อ

1. **หัวข้อไม่ลอยท้ายหน้า** — `add_body()` ใส่ `keepNext` ให้หัวข้อทุกระดับ หัวข้อจึง
   ติดไปกับสิ่งที่อยู่ถัดไปเสมอ ไม่ว่าจะเป็นย่อหน้า ตาราง หรือภาพ
   ย่อหน้านำที่สั้นกว่า `SHORT_INTRO_CHARS` (450 ตัวอักษร ราว 4 บรรทัด) ได้ `keepLines`
   เพิ่ม จะได้ย้ายไปทั้งย่อหน้าพร้อมหัวข้อ ย่อหน้ายาวกว่านั้นไม่ใส่ เพราะการบังคับ
   ย่อหน้าสิบบรรทัดให้อยู่หน้าเดียวจะดันมันไปทั้งก้อนแล้วทิ้งช่องว่างไว้ข้างหลัง
2. **ตารางสั้นไม่ฉีก** — `add_table()` ประเมินความสูงจากความกว้างคอลัมน์กับความยาว
   ข้อความ ถ้าไม่เกิน `KEEP_WHOLE_MAX_CM` (12 ซม. ราวครึ่งหน้า) จะผูกทุกแถวด้วย
   `keepNext` ตารางจึงย้ายไปทั้งก้อน
3. **ตารางยาวฉีกได้** — เกินเกณฑ์แล้วปล่อยตามเดิม หัวตารางซ้ำทุกหน้าด้วย `tblHeader`
   และไม่มีแถวไหนขาดกลางเพราะ `cantSplit`

**อย่าตั้งให้ตารางทุกขนาดห้ามฉีก** เคยลองแล้วได้ผลแย่กว่า ตารางที่ต้องใช้เกือบเต็มหน้า
ไม่มีทางอยู่ร่วมหน้ากับหัวข้อได้ มันจึงกระโดดไปหน้าถัดไปและทิ้งหัวข้อกับสามบรรทัด
ค้างไว้ที่ท้ายหน้าเดิม ส่วนการสั่ง `page_break_before` ให้หัวข้อที่ค้างยิ่งแย่กว่า
เพราะหัวข้อย้ายไปแล้วตารางก็ยังไม่พอดีอยู่ดี ได้หน้าที่ว่าง 85%

> **`doc_builder.py` ใช้ร่วมกับ SRS** การเปลี่ยนนี้ไม่ใช่แบบ opt-in เหมือนสองอย่างก่อนหน้า
> ถ้ารัน `gen_srs_docx.py` ใหม่ หน้าของ SRS จะขยับตามไปด้วย ไฟล์ที่ส่งไปแล้วไม่ถูกแตะ

## ถ้าจำเป็นต้องแก้ด้วยมือใน LibreOffice

คัดลอกออกจาก `build/` ก่อน (เช่นไป `docs/submit/`) แล้วแก้ที่สำเนานั้น
ให้สำเนากลายเป็นตัวจริงของรอบส่งนั้นไป มิฉะนั้นการรัน generator ครั้งถัดไปจะทับทิ้ง

**เซฟเป็น `.docx` (กด "Use Word 2007-365!") ไม่ต้องผ่าน `.odt`** — ทดสอบ round-trip
ไฟล์จริงแล้วทั้งสองเส้นทางให้ผลเท่ากันเป๊ะ: 40 หน้าเท่าเดิม ฟอนต์ที่ฝังชุดเดียวกัน
สารบัญยังเป็นฟิลด์จริง เลขหน้ายังเป็นฟิลด์ หน้าแนวนอน หัวตารางซ้ำข้ามหน้า และแถวที่ห้ามฉีก
อยู่ครบทั้งคู่ ODF จึงไม่ได้รักษาอะไรเพิ่ม มีแต่เพิ่มรอบแปลง

**สิ่งที่หายทั้งสองเส้นทางคือ `updateFields`** ซึ่งเป็นตัวสั่งให้ Word รีเฟรชสารบัญเองตอนเปิด
หลังแก้ในLibreOffice ต้องอัปเดตสารบัญเอง — คลิกขวาที่สารบัญ → **Update Index**
(หรือ Tools ▸ Update ▸ Update All) **ก่อน** export PDF ไม่งั้นเลขหน้าในสารบัญค้างของเดิม
และอย่าลืม export PDF จากไฟล์ที่แก้แล้ว มิฉะนั้น Word กับ PDF ที่ส่งจะไม่ตรงกัน

## สิ่งที่ต้องมีในเครื่อง

- **ฟอนต์ Sarabun** — ตัวเดียวกับที่ระบบใช้ ถ้าไม่มี Word และ LibreOffice
  จะแทนด้วยฟอนต์อื่นและหน้าจะเลื่อน ติดตั้งจาก
  <https://fonts.google.com/specimen/Sarabun> ลงใน `~/.local/share/fonts/`
  (เครื่องนี้ลิงก์ไว้ที่ `~/.fonts/Sarabun` ให้แล้ว) แล้วสั่ง `fc-cache -f`
- **LibreOffice** (`soffice`) — ใช้แปลง `.docx` เป็น `.pdf`
- **uv** — ดึง `python-docx` มาใช้ชั่วคราวโดยไม่ต้องติดตั้งถาวร

## สามเรื่องที่ต้องรู้ก่อนแก้ `doc_builder.py`

1. **ภาษาไทยใน Word เป็น "complex script"** — Word อ่านฟอนต์และขนาดของข้อความไทย
   จากแอตทริบิวต์ `w:cs` / `w:szCs` / `w:bCs` ไม่ใช่ตัวปกติ ทุกฟังก์ชันจึงเซ็ตทั้งคู่
   ถ้าลืม ข้อความไทยจะกลายเป็นฟอนต์อื่นขนาดอื่นทั้งที่ภาษาอังกฤษยังถูก
2. **ความกว้างคอลัมน์ต้องเขียนลงตารางกริด** (`w:gridCol`) ไม่ใช่แค่ที่ช่อง —
   เมื่อใช้ layout แบบคงที่ ทั้ง Word และ LibreOffice วัดจากกริด
3. **สารบัญสร้างสองรอบ** — รอบแรกได้ PDF ที่มีจำนวนบรรทัดถูกแต่เลขหน้ามั่ว
   อ่านกลับมาว่าหัวข้อไหนตกหน้าไหน แล้วสร้างใหม่ด้วยเลขจริง
   ฝั่ง Word ยังเป็นสารบัญของจริงที่กด F9 อัปเดตได้ ส่วน PDF ได้เลขหน้าที่ถูกต้อง
   เพราะ LibreOffice เรนเดอร์จากค่าที่ฝังไว้ ไม่ได้คำนวณสารบัญใหม่เอง

## ที่มาของเนื้อหา

- **SRS** — ถอดจาก `docs/resource/ULMs-SRS-v1.0-3.pdf` (ไฟล์ที่ export จาก Google Docs)
  **ความต้องการทุกข้อเป็นของทีมคำต่อคำ ห้ามแก้ถ้อยคำหรือเปลี่ยนรหัส** — รหัส FR-*/NFR-*
  ถูกอ้างตรง ๆ จาก SDS และจากกรณีทดสอบ · เปลี่ยนเฉพาะเลขหัวข้อให้ตรง IEEE 830
  และเขียนหัวข้อที่ขาดเพิ่ม
- **SDS** — ฉบับ 1.0 เขียนจาก `origin/main` ที่ `05578da` (3 ก.ย.) · ฉบับ 1.1 นับใหม่จาก `c8ed505` (10 ก.ย.) · **ฉบับ 1.2 นับใหม่จาก `0cb21a7` (17 ก.ย. 2569)**
  อ่านด้วย `git show origin/main:<path>` ไม่ได้อ่านจาก working tree ของสาขาใด
  (`schema.prisma`, `backend/src/`, `frontend/src/`, `.github/workflows/ci.yml`)
  ส่วนที่ยังไม่ได้ทำระบุไว้ในคอลัมน์ "สถานะ" และภาคผนวก ค

> **ก่อนสร้าง SDS ใหม่ทุกครั้ง ให้ fetch ก่อน แล้วอ่านจาก `origin/main`**
> ฉบับแรกเขียนจากสาขาที่ตามหลัง main อยู่ 64 commit ทำให้คอลัมน์ "สถานะ" ผิดเกือบทั้งตาราง

## ภาพในเอกสาร

อยู่ที่ [`figures/`](figures/) — use case, business object, ER และผังกระบวนการ 4 ผัง
ผังกระบวนการวางบน**หน้าแนวนอน** (บล็อก `wide_image`) เพราะกว้าง 2.4 : 1 ถ้าวางหน้าแนวตั้งจะอ่านไม่ออก

**ผัง BPMN มาจาก `docs/bpmn/png/` ไม่ได้แก้ที่ `figures/`** — ถ้าเรนเดอร์ผังใหม่ ให้ย่อลงมาที่
กว้าง 2950 px (≈ 300 dpi บนหน้าแนวนอน) ก่อนคัดลอกมา มิฉะนั้นไฟล์ที่ส่งอีเมลจะใหญ่เกินจำเป็น
ตัวเรนเดอร์ให้ภาพกว้างถึง 8560 px ซึ่งเกินที่หน้ากระดาษใช้ได้ราว 3 เท่า
