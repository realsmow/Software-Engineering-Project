/**
 * Role type.
 * The canonical definition already lives in `@/types/domain` (the backend
 * contract). Re-exported here so imports match the brief's expected path
 * without duplicating the source of truth.
 */
export type { Role } from "./domain";
