import { PlaceholderPage } from "@/components/shared/placeholder-page";

/** Placeholder route — real content pending. */
export default function StaffInspectionPage() {
  return <PlaceholderPage title="ตรวจสภาพ" />;
}
